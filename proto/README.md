# proto files

```bash
.
├── vald (mirror of vald v1 proto files)
├── payload (mirror of vald v1 payload files)
├── google (mirror of googleapis/googleapis/google)
└── protoc-gen-validate (mirror of envoyprocy/protoc-gen-validate)
```
